# Optional. Leave unset for local dev: Vite proxies /api → http://localhost:3001 (see vite.config.ts).
# Set when the API is on another origin (e.g. static site on Vercel, API on Railway):
VITE_API_URL=https://your-api.example.com
