services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: four
      POSTGRES_PASSWORD: four
      POSTGRES_DB: four
    ports:
      - "5432:5432"
    volumes:
      - four_pg:/var/lib/postgresql/data

volumes:
  four_pg:
